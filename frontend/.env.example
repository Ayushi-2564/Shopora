# Optional: Set this to your deployed backend URL in production (e.g., https://shopora-api.onrender.com/api)
# In local development, Vite proxies /api to http://localhost:5000 automatically.
VITE_API_URL=/api
