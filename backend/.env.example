PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173
# Optional: Connect MongoDB Atlas if you want database persistence, otherwise in-memory storage runs automatically with zero configuration
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/shopora?retryWrites=true&w=majority
